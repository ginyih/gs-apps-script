# clasp configuration file (contains scriptId and local project settings)
.clasp.json

# Google Apps Script project metadata (this is specific to your Google project)
appsscript.json

# Node.js dependencies (if using clasp with Node.js or npm)
node_modules/

# Log files (if you generate logs during development)
*.log

# IDE/project files (if using any IDE like VS Code or JetBrains)
.vscode/
.idea/

# OS-specific files (macOS, Windows, etc.)
.DS_Store        # macOS Finder system file
Thumbs.db        # Windows thumbnail cache
